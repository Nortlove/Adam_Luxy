# =============================================================================
# INFORMATIV Production Requirements
# Minimal set for pilot — excludes ML training, scraping, and optional packages
# =============================================================================

# ── Core Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
starlette>=0.40.0

# ── Database ──
neo4j>=5.25.0
redis>=5.0.0
aioredis>=2.0.1

# ── HTTP ──
httpx>=0.27.0
aiohttp>=3.10.0

# ── AI/ML ──
anthropic>=0.39.0
numpy>=1.26.0
scipy>=1.13.0

# ── Monitoring ──
prometheus-client>=0.21.0

# ── Logging ──
structlog>=24.4.0

# ── Utilities ──
python-dotenv>=1.0.0
python-dateutil>=2.9.0

# ── HTML Parsing (for page intelligence) ──
beautifulsoup4>=4.12.0

# ── Optional (install if using page crawler) ──
# playwright>=1.48.0
# sentence-transformers>=3.3.0
# torch>=2.5.0

# ── Optional (install if using LangGraph workflows) ──
# langgraph>=0.2.0
