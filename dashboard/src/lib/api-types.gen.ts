/**
 * Auto-generated TypeScript types from the FastAPI OpenAPI schema.
 *
 * REGENERATE: pnpm gen:types
 *   (with backend running locally on http://localhost:8000)
 *
 * Or override the source URL:
 *   OPENAPI_URL=https://informativ-backend.up.railway.app/openapi.json pnpm gen:types
 *
 * This file is checked in for two reasons:
 *   1. CI / typechecking works without the backend running
 *   2. Schema changes show up as diffs on the PR (intentional)
 *
 * Manual types in src/lib/types.ts are still authoritative for the
 * shapes components consume — those will be progressively replaced
 * by the generated types as we tighten the contract.
 */

// Placeholder until first generation. After running `pnpm gen:types`
// against a live backend, this file will be overwritten with the
// real schema-derived types.
export type paths = Record<string, never>;
export type components = {
  schemas: Record<string, never>;
};
export type operations = Record<string, never>;
