# INFORMATIV Production Dependencies

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
starlette>=0.40.0

# Database
neo4j>=5.25.0
redis>=5.0.0

# HTTP
httpx>=0.27.0

# AI
anthropic>=0.39.0
numpy>=1.26.0
scipy>=1.13.0

# Causal Inference (M2 — handoff §2.10 library pins)
# CausalForestDML — Wager-Athey 2018, Athey-Tibshirani-Wager 2019
# Soft-imported in adam.intelligence.causal_forest; missing libs raise
# LibsMissingError rather than silently returning None (drift defense).
scikit-learn>=1.4.0
econml>=0.16.0

# Hierarchical Bayes (M3 — handoff §3.6 library pins)
# Non-centered partial pooling per Papaspiliopoulos-Roberts-Sköld 2007.
# Soft-imported in adam.intelligence.hierarchical_bayes; missing libs
# raise HierarchyLibsMissingError. Task 34 (nightly refit) requires both.
pymc>=5.16
numpyro>=0.16,<0.20  # numpyro>=0.20 requires jax>=0.5 with new API
jax>=0.4.30,<0.5     # numpyro 0.16.x compat band
jaxlib>=0.4.30,<0.5

# statsmodels — econml transitive dep; >=0.14.5 needed for scipy>=1.13 compat
statsmodels>=0.14.5

# Monitoring
prometheus-client>=0.21.0

# Logging
structlog>=24.4.0

# Utilities
python-dotenv>=1.0.0
python-dateutil>=2.9.0
beautifulsoup4>=4.12.0
# Cache bust 1775880299
